# Wind's Pleasure

This is a Python script that can be run on a cron job to do
structured email processing. The main use case is removing advertising
content that is embedded directly into email bodies.

This has to be accompanied by an appropriate mailserver setup to
actually receive and proxy the mails in both directions, documentation
on that coming some day.
