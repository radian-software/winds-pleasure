#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")"

if [[ -z "${VIRTUAL_ENV:-}" ]]; then
    poetry install
    exec poetry run "$0" "$@"
fi

echo "Running at $(date)"

python3 -m winds_pleasure process-mail
curl -fsSL "${HEALTHCHECK_URL}"
echo
